<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:card_view="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">


    <android.support.v7.widget.CardView
        android:id="@+id/card_view_credit"
        android:layout_width="match_parent"
        android:layout_height="265dp"
        android:layout_gravity="center"
        android:layout_margin="5dp"
        android:elevation="@dimen/cardview_elevation"
        card_view:cardCornerRadius="4dp">

        <android.support.constraint.ConstraintLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/credit_poster_image"
                android:layout_width="150dp"
                android:layout_height="200dp"
                android:layout_gravity="center"
                android:layout_marginStart="0dp"
                android:src="@drawable/no_image" />

            <TextView
                android:id="@+id/credit_name"
                android:layout_width="150dp"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:paddingEnd="@dimen/between_elements"
                android:paddingStart="@dimen/between_elements"
                android:textSize="@dimen/title_size"
                android:textColor="@color/colorPrimary"
                app:layout_constraintTop_toBottomOf="@id/credit_poster_image" />

            <TextView
                android:id="@+id/credit_character"
                android:layout_width="150dp"
                android:layout_height="wrap_content"
                android:paddingEnd="@dimen/between_elements"
                android:paddingStart="@dimen/between_elements"
                android:textSize="14sp"
                app:layout_constraintTop_toBottomOf="@id/credit_name" />


        </android.support.constraint.ConstraintLayout>

    </android.support.v7.widget.CardView>

</LinearLayout>